# prahari-backend

## Setup
1. clone repo
2. create venv: `python -m venv venv`
3. activate venv
4. pip install -r requirements.txt
5. copy .env.example to .env
6. run migrations
7. run dev server: `uvicorn app.main:app --reload`
